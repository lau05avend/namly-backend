/** Minimum meal logs registered on a day to count toward streak. */
export const MIN_MEALS_FOR_STREAK = 1;

/** Week starts on Monday (JavaScript getUTCDay: 0 = Sunday, 1 = Monday). */
export const WEEK_START_DAY = 1;
